gcc 4.7.2 -O2 places some string on the stack in 

new QAction("string", window)

so it crashes on strlen because stack is messed up. 
for some reason another 
new QAction("", window) 
fixes it or
new QAction(*new QString("string"), window)

Use -O0 to fix it for now, ie QT_CXXFLAGS += debug
